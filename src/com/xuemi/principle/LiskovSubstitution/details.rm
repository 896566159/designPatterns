里氏替换原则：
OO中的继承：
1）含义：范磊中已经实现好的方法没实际上是在设定规范和契约，虽然不强制要求所有的子类比逊遵循这些契约，
  但是如果子类对这些已经实现的方法进行任意修改，会造成对整个继承体系的破坏
2）继承再给程序设计带来便利的同时，也带来了弊端。增加了对象间的耦合性，如果一个类被其他类所继承，则当修改该类时，必须考虑到所有子类是否有影响
3）继承必须遵循里氏替换原则

里斯替换原则原理：
1）对于每个类型为 T1的对象O1， 都有：类型为T2的对象O2，使得以T1定义的所有程序P在所有的对象O1队替换成O2时，程序P没有收到影响，则类型T2是T1的子类。
即：所有引用积累的地方必须能够透明的使用其子类的对象。
2）使用继承时，遵循里氏替换原则，在子类中尽量不要重写分类的方法
3）继承增强了耦合性，可以通过聚合、组合、依赖来避免继承