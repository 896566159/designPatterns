原型模式（创建型）总结：
1）原型模式的意义：
    创建新对象时，尤其是复杂的对象，利用原型模式可以简化创建过程，提高效率
2）在创建对象时，可以动态的获取独享运行时的状态
3）原始对象成员变量增加时，其克隆对象也会拥有新的成员变量，无需对新增的成员变量修改
4）缺点：1）在实现深克隆（从写clone()方法、序列化与反序列化）时，实现代码复杂
        2）如果某个类需要作为prototype原型类，则都需要实现深克隆方法，对于已经存在的类而言，需要修改其源码，违背了OCP原则
5）典例：Spring中创建bean对象时，有prototype方式
    <bean id="id01" class="com.atguigu.spring.bean.Monster" scope="prototype"/>