#简单工厂模式——静态工厂模式

引用案例：生产披萨
改进思路分析：
    1）在OrderPizza类中创建pizza对象是，需要修改的地方比较多，可以将创建对象的过程封装在一个专门用于创建pizza的类中
    2）改进后的简单工厂模式，还需在OrderPizza类中声明simpleFactory类对象，并调用其创建Pizza对象的方法CreatePizza，
       故这次改进将把创建对象的方法和返回的对象静态化，直接通过类来调用方法
